/* MATRIX PRO INVESTING - STYLE */

@import url('https://fonts.googleapis.com/css2?family=Share+Tech+Mono&display=swap');

body {
  margin: 0;
  overflow: hidden;
  font-family: 'Share Tech Mono', monospace;
  background-color: #0a0f0f;
  color: #00ffd9;
}

/* Canvas animato sullo sfondo */
canvas#matrix-bg {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 0;
  pointer-events: none;
}

/* Contenuto principale */
.container {
  position: relative;
  z-index: 1;
  background: rgba(0, 0, 0, 0.7);
  border: 1px solid #00ffaa;
  border-radius: 10px;
  box-shadow: 0 0 20px rgba(0, 255, 170, 0.5);
  width: 90%;
  max-width: 900px;
  margin: 60px auto;
  padding: 20px;
  color: #00ffd9;
}

/* Titolo */
h1, h2 {
  color: #00ffcc;
  text-align: center;
  text-shadow: 0 0 10px #00ffaa;
  margin-bottom: 20px;
}

/* Tabelle e pulsanti */
table {
  width: 100%;
  border-collapse: collapse;
  margin-top: 10px;
}

th, td {
  border: 1px solid #00ffaa;
  padding: 8px;
  text-align: center;
}

button, input {
  background: #000;
  color: #00ffaa;
  border: 1px solid #00ffaa;
  border-radius: 5px;
  padding: 10px;
  margin-top: 5px;
  font-family: inherit;
  transition: 0.3s;
}

button:hover {
  background: #00ffaa;
  color: #000;
  cursor: pointer;
}

/* Sezioni */
.section {
  margin-top: 30px;
  padding: 15px;
  border: 1px solid #00ffaa;
  border-radius: 5px;
}

/* Chat Matrix Assistant */
.chat-box {
  height: 250px;
  overflow-y: auto;
  background: rgba(0, 0, 0, 0.8);
  padding: 10px;
  border-radius: 5px;
  border: 1px solid #00ffaa;
  font-size: 14px;
}

.chat-input {
  display: flex;
  gap: 5px;
  margin-top: 10px;
}

.chat-input input {
  flex: 1;
}

