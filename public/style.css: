/* =========================
   MATRIX PRO INVESTING STYLE
   ========================= */

/* Font futuristico */
@import url('https://fonts.googleapis.com/css2?family=Share+Tech+Mono&display=swap');

/* Sfondo generale */
body {
  margin: 0;
  padding: 0;
  font-family: 'Share Tech Mono', monospace;
  background-color: #000;
  color: #00ff9d;
  overflow-x: hidden;
}

/* Effetto matrix background animato */
body::before {
  content: "";
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: url('https://i.ibb.co/4f7Q5c9/matrix-bg.gif') center center / cover no-repeat;
  opacity: 0.2;
  z-index: -1;
}

/* Titolo principale */
h1, h2, h3 {
  color: #00ff9d;
  text-align: center;
  text-shadow: 0 0 10px #00ff9d;
}

/* Contenitore principale */
.container {
  background: rgba(0, 0, 0, 0.85);
  border: 1px solid #00ff9d;
  border-radius: 10px;
  box-shadow: 0 0 25px rgba(0, 255, 157, 0.4);
  width: 85%;
  max-width: 900px;
  margin: 50px auto;
  padding: 30px;
}

/* Tabelle Bonus ADM */
table {
  width: 100%;
  border-collapse: collapse;
  margin-top: 15px;
  background-color: rgba(0, 0, 0, 0.6);
  border: 1px solid #00ff9d;
  border-radius: 10px;
  overflow: hidden;
}

th, td {
  border: 1px solid #00ff9d;
  padding: 10px;
  text-align: center;
}

th {
  background-color: rgba(0, 255, 157, 0.1);
  color: #00ff9d;
  font-weight: bold;
}

tr:nth-child(even) {
  background-color: rgba(0, 255, 157, 0.05);
}

tr:hover {
  background-color: rgba(0, 255, 157, 0.15);
}

/* Sezione amici */
ul {
  list-style: none;
  padding: 0;
}

ul li::before {
  content: ">";
  color: #00ff9d;
  margin-right: 8px;
}

/* Matrix Assistant box */
.chat-box {
  border: 1px solid #00ff9d;
  border-radius: 10px;
  padding: 15px;
  background-color: rgba(0, 0, 0, 0.6);
  margin-top: 25px;
}

.chat-log {
  background: rgba(0, 255, 157, 0.05);
  border: 1px solid #00ff9d;
  border-radius: 5px;
  padding: 10px;
  height: 200px;
  overflow-y: auto;
  margin-bottom: 15px;
  font-size: 14px;
  line-height: 1.5;
}

/* Input e pulsanti */
input[type="text"], input[type="password"] {
  width: 80%;
  padding: 10px;
  border: 1px solid #00ff9d;
  border-radius: 5px;
  background-color: #000;
  color: #00ff9d;
  outline: none;
  font-family: 'Share Tech Mono', monospace;
}

button, input[type="submit"] {
  padding: 10px 20px;
  border: 1px solid #00ff9d;
  background-color: #000;
  color: #00ff9d;
  font-family: 'Share Tech Mono', monospace;
  cursor: pointer;
  border-radius: 5px;
  transition: 0.3s;
}

button:hover, input[type="submit"]:hover {
  background-color: #00ff9d;
  color: #000;
}

/* Link e logout */
a {
  color: #00ff9d;
  text-decoration: none;
}

a:hover {
  text-shadow: 0 0 5px #00ff9d;
}

/* Footer */
footer {
  text-align: center;
  color: #00ff9d;
  opacity: 0.6;
  font-size: 12px;
  margin-top: 30px;
}
