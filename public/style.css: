@import url('https://fonts.googleapis.com/css2?family=Share+Tech+Mono&display=swap');

body {
  margin: 0;
  font-family: 'Share Tech Mono', monospace;
  background-color: #000;
  color: #00ff9d;
  overflow-x: hidden;
}

#matrix-bg {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 0;
  pointer-events: none;
}

.container {
  position: relative;
  z-index: 1;
  background: rgba(0,0,0,0.75);
  border: 1px solid #00ff9d;
  border-radius: 10px;
  box-shadow: 0 0 25px rgba(0,255,157,0.3);
  width: 90%;
  max-width: 1100px;
  margin: 50px auto;
  padding: 25px;
  text-align: center;
}

h1, h2 {
  color: #00ff9d;
  text-shadow: 0 0 10px #00ff9d;
}

.actions {
  margin: 20px 0;
}

button {
  background-color: #000;
  color: #00ff9d;
  border: 1px solid #00ff9d;
  border-radius: 5px;
  padding: 10px 20px;
  cursor: pointer;
  transition: 0.3s;
}
button:hover {
  background-color: #00ff9d;
  color: #000;
  box-shadow: 0 0 10px #00ff9d;
}

/* Tabella */
table {
  width: 100%;
  border-collapse: collapse;
  margin-top: 10px;
}
th, td {
  border: 1px solid #00ff9d;
  padding: 10px;
  text-align: center;
}
th {
  background: rgba(0,255,157,0.1);
  color: #00ff9d;
}
tr:nth-child(even) {
  background: rgba(0,255,157,0.05);
}
tr:hover {
  background: rgba(0,255,157,0.15);
}

/* Categorie colorate */
.cat.sport { color: #00ff9d; }
.cat.casinò { color: #00bfff; }
.cat.fintech { color: #fff700; }

/* Stato colorato */
.stato.attivo { color: #00ff9d; }
.stato.scadenza, .stato['in scadenza'] { color: #ffaa00; }
.stato.scaduto { color: #ff5555; }

footer {
  text-align: center;
  color: #00ff9d;
  opacity: 0.6;
  font-size: 13px;
  margin-top: 30px;
}
