@import url('https://fonts.googleapis.com/css2?family=Share+Tech+Mono&display=swap');

body {
  margin: 0;
  overflow-x: hidden;
  background-color: #0a0f0f;
  font-family: 'Share Tech Mono', monospace;
  color: #00ffd9;
}

/* Canvas sfondo Matrix */
#matrix-bg {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 0;
  pointer-events: none;
}

/* Contenitori principali */
.container {
  position: relative;
  z-index: 1;
  background: rgba(0, 0, 0, 0.7);
  border: 1px solid #00ffaa;
  border-radius: 10px;
  box-shadow: 0 0 25px rgba(0, 255
